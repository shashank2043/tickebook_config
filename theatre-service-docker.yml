spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Shashank@123
    url: jdbc:mysql://mysql:3306/theatre_db?createDatabaseIfNotExist=true
    username: shashank
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
server:
  port: 0
jwt:
  secret: tickebook_auth_jwt_super_secure_secret_2026_value
resilience4j:
  circuitbreaker:
    instances:
      ADMIN-SERVICE:
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s